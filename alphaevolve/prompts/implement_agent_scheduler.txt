{# Jinja2 template for a standalone agent-generated XLS scheduler #}
== CURRENT TASK ==
Implement a new standalone XLS scheduling algorithm in `{{ target_file_path }}` by
writing the complete body of `{{ mutation_target }}`.

This function is selected by XLS when `--scheduling_strategy=agent` is used.
Your code must return a legal `ScheduleCycleMap` for the given design.

== DESIGN GOAL ==
Create a real scheduling algorithm, not a parameter tweak:
- combine ideas from existing XLS schedulers and literature into a hybrid heuristic
- change how nodes are prioritized / packed / spread across stages
- aim to improve generated hardware PPA by changing the schedule itself

Think in AlphaEvolve-style terms:
- produce a distinct heuristic family, not just small constant changes
- keep the algorithm deterministic and compilable
- reuse existing XLS bounds/constraint machinery rather than rebuilding all infrastructure
- prefer a coherent strategy such as pressure-aware list scheduling, mobility-aware
  placement, multi-pass refinement, or greedy schedule repair
- be innovative in the heuristic, but disciplined in implementation size and runtime
- prefer one coherent algorithmic family over a giant hybrid of many unrelated ideas
- do NOT fall back to trivial ASAP-only behavior unless that is clearly justified by
  a stronger scoring/tie-breaking scheme
- avoid bloated helper duplication, multiple abandoned scoring families, or heavy
  nested search that is likely to time out on larger designs
- bounded lookahead or one repair pass is good; unbounded multi-pass exploration is not

Use all of your relevant prior knowledge as well:
- the `knowledge/` excerpts are helpful context, not an exclusive whitelist
- you may draw on general graph algorithms, scheduling theory, compiler heuristics,
  optimization intuition, and standard hardware-scheduling ideas you already know
- if the local knowledge is incomplete, still propose a principled algorithm based
  on sound fundamentals rather than limiting yourself to only the quoted notes

Runtime expectations:
- target a practical heuristic that can run on larger graphs such as GEMM-style designs
- prefer roughly single-pass topological scheduling with bounded per-node candidate scoring
- be careful with repeated expensive recomputation after every choice

== HARDWARE CONSTRAINTS ==
Clock period:    {{ clock_period_ps }} ps  (each pipeline stage must fit within this budget)
Pipeline stages: {% if pipeline_stages is not none %}{{ pipeline_stages }} (FIXED — you MUST distribute nodes across exactly {{ pipeline_stages }} stages){% else %}not fixed — let clock period and bounds determine the stage count{% endif %}

Delay model: unit delay means each node contributes 1 ps.
With clock_period={{ clock_period_ps }} ps and unit model, up to {{ clock_period_ps }} nodes can fit in
one stage. If you schedule everything in 1 stage your score is dominated by area+delay,
not by stage count — but pipelining reduces area and timing pressure, so consider spreading
nodes across stages even when the clock allows fitting them all in one stage.

== CURRENT PPA SCORE ==
{% if best_score is not none %}
Best score so far: {{ '%.4f' | format(best_score) }} (lower is better)
  ↳ {{ best_num_stages }} pipeline stages, {{ best_reg_bits }} register bits, {{ best_delay_ps }}ps critical path
{% else %}
Best score so far: none yet — you are the first iteration, aim to produce a valid schedule.
{% endif %}
Parent candidate score: {{ '%.4f' | format(parent_score) }}
  ↳ {{ parent_num_stages }} stages, {{ parent_reg_bits }} register bits, {{ parent_delay_ps }}ps

{% if baseline_benchmark_context %}
== BASELINE XLS BENCHMARK RESULTS ==
These are benchmark_main results from the original XLS scheduler (not the agent).
Use them as a reference to understand what PPA your algorithm should aim to beat.
{{ baseline_benchmark_context }}
{% endif %}

{% if previous_attempt_feedback %}
== PREVIOUS ATTEMPT FAILED TO PRODUCE USEFUL PPA ==
Status: {{ previous_attempt_feedback.status }}
Reason: {{ previous_attempt_feedback.reason }}
{% if previous_attempt_feedback.num_stages is not none %}
Observed stage count: {{ previous_attempt_feedback.num_stages }}
{% endif %}
{% if previous_attempt_feedback.min_clock_period_ps is not none %}
Observed minimum feasible clock: {{ previous_attempt_feedback.min_clock_period_ps }} ps
{% endif %}
{% if previous_attempt_feedback.max_stage_delay_ps is not none %}
Observed max stage delay: {{ previous_attempt_feedback.max_stage_delay_ps }} ps
{% endif %}
{% if previous_attempt_feedback.runtime_s is not none %}
Observed scheduler runtime: {{ '%.1f' | format(previous_attempt_feedback.runtime_s) }} s
{% endif %}
{% if previous_attempt_feedback.likely_under_pipelined %}
Diagnosis: likely under-pipelined at the requested clock. Spread nodes across more stages / later legal cycles instead of over-packing early stages.
{% endif %}
{% if previous_attempt_feedback.guidance %}
Guidance: {{ previous_attempt_feedback.guidance }}
{% endif %}
{% endif %}

== MUTATION INSTRUCTION ==
{{ mutation_instruction }}

== RELEVANT ALGORITHMS FROM LITERATURE ==
{{ knowledge_context }}

== CURRENT SCHEDULER SOURCE ==
{{ current_source }}

== REFERENCE XLS SOURCES ==
Use these to understand the API contract, available helpers, and how other
schedulers behave. You may borrow ideas, but produce your own coherent
algorithm.
{{ reference_source_bundle }}

== STRICT OUTPUT RULES ==
Your output will be spliced directly into `{{ target_file_path }}` at the exact
location of the target function.

You MUST follow these rules or the code will not compile:
1. CRITICAL: Do NOT add `#include` lines.
2. CRITICAL: Do NOT wrap the output in `namespace xls {}`.
3. Preserve the function signature exactly.
4. You may add helper functions before the main function, preferably in an
   anonymous `namespace {}` block.
5. Use only identifiers and APIs visible in the current source and references.
6. Prefer existing XLS utilities over inventing new infrastructure.
7. Do not emit markdown fences or prose.

Bad output examples that will fail immediately:
- `#include "xls/..."`
- `namespace xls {`
- `````cpp`

Why this is strict:
- the output is inserted in the middle of an existing `.cc` file
- the file already has the required includes
- the file is already inside `namespace xls`

Correct output shape:
- optional anonymous `namespace {}` helpers
- then the full `AgentGeneratedScheduler(...)` definition
- raw C++ only

Compile-safety guidance:
- Keep the algorithm self-contained inside this file.
- Prefer standard XLS/Abseil containers and helper routines already used nearby.
- If you need staged assignment, work through `sched::ScheduleBounds` and return
  a final `ScheduleCycleMap`.
- If retrying after a compiler failure, fix the specific C++ issue first and only
  then change algorithm details.

== CRITICAL API PATTERNS — MEMORISE THESE OR YOUR CODE WILL NOT COMPILE ==

// ──────────────────────────────────────────────────────────────
// TopoSort — MOST COMMON COMPILE ERROR
// TopoSort(f) returns absl::StatusOr<std::vector<Node*>>, NOT a vector.
// You MUST unwrap it with XLS_ASSIGN_OR_RETURN before iterating.
// ──────────────────────────────────────────────────────────────

// ✓ CORRECT
XLS_ASSIGN_OR_RETURN(std::vector<Node*> nodes, TopoSort(f));
for (Node* node : nodes) { /* ... */ }

// ✗ COMPILE ERROR — do NOT write this:
for (Node* node : TopoSort(f)) { /* WRONG: StatusOr is not iterable */ }

// ──────────────────────────────────────────────────────────────
// bounds methods — all return absl::Status; you MUST check them
// ──────────────────────────────────────────────────────────────

XLS_RETURN_IF_ERROR(bounds->TightenNodeLb(node, cycle));
XLS_RETURN_IF_ERROR(bounds->TightenNodeUb(node, cycle));
XLS_RETURN_IF_ERROR(bounds->PropagateBounds());

// ──────────────────────────────────────────────────────────────
// delay_estimator — returns StatusOr; use helper pattern
// ──────────────────────────────────────────────────────────────

absl::StatusOr<int64_t> d_or = delay_estimator.GetOperationDelayInPs(node);
int64_t node_delay_ps = d_or.ok() ? d_or.value() : 0;

// ──────────────────────────────────────────────────────────────
// pipeline_stages — USE THIS, do NOT ignore it with (void)pipeline_stages
// pipeline_stages is the REQUIRED number of pipeline stages for this design.
// Your schedule MUST use EXACTLY pipeline_stages cycles (0 … pipeline_stages-1).
// Ignoring pipeline_stages collapses the design to 1 stage, wasting all
// pipeline-register reduction potential and scoring poorly.
// ──────────────────────────────────────────────────────────────

// ✓ Example: use pipeline_stages to spread nodes across stages
// lb = bounds->lb(node); ub = min(bounds->ub(node), pipeline_stages - 1);
// Then pick a cycle in [lb, ub] using your scoring heuristic.

{% if compile_error %}
== PREVIOUS ATTEMPT FAILED TO COMPILE ==
Compiler output:
{{ compile_error }}

Fix every compiler issue before writing the next version.
{% endif %}

== YOUR OUTPUT ==
Write raw C++ only: any helper `namespace {}` block first, then the complete
definition of `{{ mutation_target }}`.
